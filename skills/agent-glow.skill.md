---
name: agent-glow
description: Adds glow, bloom and light beam rendering effects for canvas objects.
---

# Glow Effects

## Commands
agent-glow init
agent-glow add outline
agent-glow add bloom

## API
drawGlowCircle(x,y,r,color)
drawLightBeam(x,y,w,h,color)